services:
  meetings-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BASE_PATH: "/meetings"
        NEXT_PUBLIC_BASE_PATH: "/meetings"
    container_name: vivonline-meetings
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
      # Subpath hosting variables
      - BASE_PATH=/meetings
      - NEXT_PUBLIC_BASE_PATH=/meetings
      # External FastAPI transcription service on VDS
      - FASTAPI_API_URL=http://157.22.175.215:8000
      # OpenAI API key and model for normalization and protocols
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-5.6-luna}
      # Persistent data folder
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
